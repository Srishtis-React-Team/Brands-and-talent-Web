import { useNavigate } from "react-router";
import Header from "../layout/header";
import Footer from "../layout/Footer";
const Guidelines = () => {
  const navigate = useNavigate();
  return (
    <>
      <Header />
      Guidelines Page under Development
      <Footer />
    </>
  );
};

export default Guidelines;
